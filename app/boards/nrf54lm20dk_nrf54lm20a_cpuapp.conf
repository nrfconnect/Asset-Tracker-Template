#
# Copyright (c) 2026 Nordic Semiconductor ASA
#
# SPDX-License-Identifier: LicenseRef-Nordic-5-Clause
#

# --- UART configuration for Serial Modem ---
CONFIG_UART_ASYNC_API=y

# --- Zephyr modem subsystem (CMUX+PPP+chat, no modem_cellular driver) ---
CONFIG_MODEM=y
CONFIG_MODEM_MODULES=y
CONFIG_MODEM_BACKEND_UART=y
CONFIG_MODEM_BACKEND_UART_ASYNC=y
CONFIG_MODEM_BACKEND_UART_ASYNC_HWFC=y
CONFIG_MODEM_CMUX=y
CONFIG_MODEM_CMUX_MTU=127
CONFIG_MODEM_CMUX_WORK_BUFFER_SIZE_EXTRA=2048
CONFIG_MODEM_CHAT=y
CONFIG_MODEM_PPP=y
CONFIG_MODEM_STATS=y
CONFIG_MODEM_LOG_LEVEL_INF=y
CONFIG_MODEM_CMUX_LOG_LEVEL_DBG=y
CONFIG_MODEM_MODULES_LOG_LEVEL_DBG=y

# --- nRF Modem Library proxy over Zephyr modem subsystem ---
CONFIG_NRF_MODEM_LIB=y
CONFIG_NRF_MODEM_LIB_ZEPHYR_MODEM=y
CONFIG_NRF_MODEM_LIB_ZEPHYR_MODEM_UART_BUF_SIZE=512

# --- Logging: immediate mode with UART backend ---
# With LOG_MODE_DEFERRED the log processing thread runs at K_LOWEST_APPLICATION_THREAD_PRIO
# and is starved by the networking/AT-proxy threads; the only way output appeared during
# the bootloop was LOG_PANIC() forcing a synchronous flush on the fatal error path.
# Immediate mode prints every message inline in the calling thread — reliable for bring-up.
CONFIG_LOG_MODE_IMMEDIATE=y
# # SHELL_BACKEND_SERIAL=y causes LOG_BACKEND_UART to default to n (the shell log backend
# # takes over). Force it back on so log output goes directly to uart_poll_out.
# CONFIG_LOG_BACKEND_UART=y
# # LOG_BACKEND_UART_ASYNC=y is auto-selected when UART_ASYNC_API=y. The async backend
# # buffers 32 bytes then DMAs to UART; if the device resets before TX completes the buffer
# # is silently dropped. Force synchronous uart_poll_out so every character is written
# # before the calling code continues — mandatory for reliable crash diagnostics.
# CONFIG_LOG_BACKEND_UART_ASYNC=n
# # Decouple printk() from the LOG system. Without this, printk (including the NCS boot
# # banner) is routed through LOG — and if the shell log backend hasn't started yet,
# # the message is silently dropped. With this disabled, printk goes directly to
# # uart_console_out -> uart_poll_out regardless of LOG backend state.
# CONFIG_LOG_PRINTK=n
# # With LOG_BACKEND_UART=y added above, both UART and shell log backends would be active
# # on the same uart20, causing every log line to appear twice. Disable the shell backend.
# CONFIG_SHELL_LOG_BACKEND=n

# # --- Fault diagnostics ---
# # Print full CPU register dump + stack on any fault (ARM Cortex-M fault handler).
# CONFIG_FAULT_DUMP=2
# # Halt on fatal error instead of resetting so output can drain from the UART
# # before the hardware WDT fires. Remove once the crash is identified.
# CONFIG_RESET_ON_FATAL_ERROR=n

# --- LTE libraries (via AT proxy) ---
CONFIG_LTE_LINK_CONTROL=y
CONFIG_MODEM_INFO=y
CONFIG_NRF_MODEM_LIB_CFUN_HOOKS=y

# --- PPP networking ---
CONFIG_NET_NATIVE=y
CONFIG_NET_L2_PPP=y
CONFIG_NET_SOCKETS_OFFLOAD=n
CONFIG_NET_UDP=y
CONFIG_NET_TCP=y
CONFIG_NET_CONTEXT_RCVTIMEO=y
CONFIG_NET_CONTEXT_SNDTIMEO=y

# --- DNS (using PPP DNS option) ---
CONFIG_DNS_RESOLVER=y
CONFIG_NET_L2_PPP_OPTION_DNS_USE=y

# --- TLS/DTLS (native, not modem-offloaded) ---
CONFIG_NET_SOCKETS_SOCKOPT_TLS=y
CONFIG_NET_SOCKETS_ENABLE_DTLS=y
CONFIG_NET_SOCKETS_TLS_MAX_CONTEXTS=1
CONFIG_NET_SOCKETS_TLS_SET_MAX_FRAGMENT_LENGTH=y

# --- Location module ---
CONFIG_NRF_CLOUD_AGNSS=n
CONFIG_LOCATION_METHOD_GNSS=n
CONFIG_LOCATION_METHOD_WIFI=n
CONFIG_LOCATION_METHOD_CELLULAR=y
CONFIG_LOCATION_REQUEST_DEFAULT_METHOD_FIRST_CELLULAR=y
CONFIG_LOCATION_REQUEST_DEFAULT_METHOD_SECOND_NONE=y
CONFIG_LOCATION_REQUEST_DEFAULT_METHOD_THIRD_NONE=y

# --- FOTA: stub only (NRF_CLOUD_FOTA_POLL requires SOC_SERIES_NRF91 assumptions
# around full modem update; MCUboot swap is not wired up for nRF54L yet).
# APP_FOTA stays enabled so the zbus channel and state machine handlers in
# main.c continue to compile; the stub just publishes FOTA_MODULE_READY.
CONFIG_NRF_CLOUD_FOTA_POLL=n
CONFIG_NRF_CLOUD_FOTA_POLL_LOG_LEVEL_DBG=n
CONFIG_NRF_CLOUD_FOTA_FULL_MODEM_UPDATE=n
CONFIG_NRF_CLOUD_COAP_DOWNLOADS=n
CONFIG_FOTA_DOWNLOAD=n
CONFIG_BOOTLOADER_MCUBOOT=n
CONFIG_IMG_MANAGER=n
CONFIG_MCUBOOT_IMG_MANAGER=n
CONFIG_DFU_TARGET=n
CONFIG_DOWNLOADER=n
CONFIG_NRF_CLOUD_SEND_SERVICE_INFO_FOTA=n

# --- Memory adjustments ---
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=1536
CONFIG_HEAP_MEM_POOL_SIZE=12288
CONFIG_MAIN_STACK_SIZE=2560

# --- MbedTLS configuration (native TLS, no TF-M offload) ---
CONFIG_NRF_SECURITY=y
CONFIG_MBEDTLS=y
CONFIG_MBEDTLS_LEGACY_CRYPTO_C=y
CONFIG_MBEDTLS_ENABLE_HEAP=y
CONFIG_MBEDTLS_PEM_PARSE_C=y
CONFIG_MBEDTLS_SSL_KEEP_PEER_CERTIFICATE=n
CONFIG_MBEDTLS_SSL_SERVER_NAME_INDICATION=y
CONFIG_MBEDTLS_RSA_C=y
CONFIG_MBEDTLS_HEAP_SIZE=51200

# PSA Crypto options
CONFIG_PSA_WANT_ALG_ECDH=y
CONFIG_PSA_WANT_ALG_ECDSA=y
CONFIG_PSA_WANT_ECC_SECP_R1_256=y
CONFIG_PSA_WANT_ECC_SECP_R1_384=y
CONFIG_PSA_WANT_KEY_TYPE_ECC_KEY_PAIR_IMPORT=y
CONFIG_PSA_WANT_KEY_TYPE_ECC_KEY_PAIR_EXPORT=y
CONFIG_PSA_WANT_KEY_TYPE_ECC_KEY_PAIR_GENERATE=y
CONFIG_PSA_WANT_KEY_TYPE_ECC_PUBLIC_KEY=y

CONFIG_APP_NETWORK_THREAD_STACK_SIZE=4096
CONFIG_MAIN_STACK_SIZE=32684
CONFIG_LTE_LC_WORKQUEUE_STACK_SIZE=2048
CONFIG_DATE_TIME_THREAD_STACK_SIZE=4096
CONFIG_DATE_TIME_NTP=n
