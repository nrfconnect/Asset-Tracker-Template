# Provisioning
 - When the provisioning command is sent to the device shadow, the device will continously poll
   for provisioning commands until provisioning has completed.
 - The device can be rebooted by issuing the reboot command
 - Document how the device can be provisioned how the process goes
 - Provide REST calls to perform claiming, deleting, and resetting and why its important to roll credentials.
 - Document about too many commands, heap expensive